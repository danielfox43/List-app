<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <style>
      .form form input{
        display: block;
        margin-top: 8px;
        padding: 15px 20px;
        border-radius: 5px;
        border: 1px solid #1f2133;
        background: none;
      }
      .form form input:focus {
        outline: #1f21333a solid 3px;
      }
      .form form {
        display: inline-block;
      }
      body, html {
        font-family: system-ui, -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Oxygen, Ubuntu, Cantarell, 'Open Sans', 'Helvetica Neue', sans-serif;
        margin: 0;
        width: 100%;
        height: 100%;
        background-color: #e5e5f7;
  opacity: 1;
  background-image: repeating-radial-gradient(
      circle at 0 0,
      transparent 0,
      #e5e5f7 100px
    ),
    repeating-linear-gradient(#95b0ce8a, #c7b2e4);
}
      .form-container {
        border-right: 1px solid #1f2133;
        align-items: start;

        justify-items: center;
        display: grid;
        width: fit-content;
        background-color: white;
        width: 70%;
        height: 100%;
      }
      .description {
        max-width: 80%;
        text-align: center;
        text-shadow: 2px 2px #1f21332a ;
        border-radius: 5px;
        margin-top: 10px;
      }
      .form {
        width: fit-content;
        display: inline-block;

      }
      label {
        font-weight: bold;
      }
      h5 {
        margin: 4px;
        color: rgb(255, 82, 82);
      }
    </style>
</head>
<body>
 <div class="form-container">
  <div class="description"><h1>Welcome<br>
    Sign up into list app</h1>
</div>
    <div class="form">
        <form action="./login" method="POST">
        <label>Email:</label>
        <input id="email" type="email" placeholder="Enter your email" name="email"required>
        <label>Password:</label>
        <input id="pass1" type="password" placeholder="Enter your password" name="password" required>
        <input type="checkbox" name="remember" style="display: inline-block;">Remember me
        <h5><%= message%></h5>
        <input type="submit" value="Sign up">
        <p style="font-size: small; color: #1f2133bd;">Don't have an account yet? <a href="./register">Sign up here</a></p>
        </form>
 </div>
</body>
</html>